/**
 * Data Transfer Objects for the REST API request and response payloads.
 *
 * <p>Defines the external, versionable transport contract consumed and produced
 * by {@link com.vw.eacontext.api controllers}, together with any Jakarta Bean
 * Validation constraints. DTOs deliberately mirror API needs rather than the
 * internal {@link com.vw.eacontext.model domain model}, allowing the two to
 * evolve independently.</p>
 */
package com.vw.eacontext.dto;

