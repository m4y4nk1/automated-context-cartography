/**
 * Graph engine: builds and traverses the architecture graph using JGraphT.
 *
 * <p>Transforms the canonical {@link com.vw.eacontext.model domain model} into
 * a JGraphT graph and exposes graph construction, queries and algorithms
 * (traversal, connectivity, shortest paths, cycles, centrality, etc.). This
 * package encapsulates the JGraphT dependency so the rest of the application
 * works against domain-level graph abstractions.</p>
 */
package com.vw.eacontext.graph;

