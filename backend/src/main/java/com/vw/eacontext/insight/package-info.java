/**
 * Insight layer: detection and analysis of architecturally significant
 * patterns over the {@link com.vw.eacontext.graph graph engine}.
 *
 * <p>Contains rules and detectors that surface findings such as orphaned
 * elements, cyclic dependencies, single points of failure, bounded-context
 * boundary violations and other heuristics. Produces structured insights that
 * can be exposed via the API or enriched by the AI layer.</p>
 */
package com.vw.eacontext.insight;

