/**
 * Configuration: Spring {@code @Configuration} classes and typed properties.
 *
 * <p>Centralizes application wiring and cross-cutting setup such as bean
 * definitions, {@code @ConfigurationProperties}, JSON/Jackson customization,
 * HTTP clients and AI provider settings. Keeps infrastructure configuration
 * separate from business logic.</p>
 */
package com.vw.eacontext.config;

