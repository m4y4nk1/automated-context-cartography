package com.vw.eacontext.model;

/** Technical integration protocol of an {@link Interface}. */
public enum Protocol {
    REST_HTTPS,
    SOAP,
    GRAPHQL,
    SFTP_FILE,
    KAFKA,
    JDBC,
    GRPC
}
