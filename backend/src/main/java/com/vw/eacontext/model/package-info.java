/**
 * Canonical domain model for the Enterprise Architecture context cartography.
 *
 * <p>Contains the technology-agnostic, immutable-friendly domain entities that
 * represent the "single source of truth" after ingestion (e.g. architecture
 * elements, relationships, contexts and their attributes). Types here have no
 * dependency on parsing formats, persistence, transport (DTOs) or the graph
 * engine, so they can be shared freely across all other packages.</p>
 */
package com.vw.eacontext.model;

